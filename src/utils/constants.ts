export const OVERLAY_CLASS = "schreibstube-overlay";

/** Dispatched on `window` when the glossary selection for the active note
 *  changes, so the editor underline can rebuild without polling. */
export const GLOSSARY_CHANGED_EVENT = "schreibstube-glossary-changed";
